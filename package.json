{
  "name": "streamdeck-pihole",
  "version": "1.0.0",
  "description": "",
  "main": "us.johnholbrook.pihole.sdPlugin/bin/plugin.js",
  "scripts": {
    "build": "esbuild src/plugin.js --bundle --format=cjs --outfile=us.johnholbrook.pihole.sdPlugin/bin/plugin.js --platform=node --target=node20",
    "postinstall": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "npm run build -- --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@elgato/streamdeck": "^1.4.1"
  },
  "devDependencies": {
    "esbuild": "^0.27.1"
  }
}
